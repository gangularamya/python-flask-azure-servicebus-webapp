# Azure Service Bus Configuration Example - Zero Secrets Architecture 🔐
# Copy this file to .env and update with your actual values

# Service Bus Namespace (without protocol, just the FQDN)
SERVICE_BUS_NAMESPACE=your-namespace.servicebus.windows.net
QUEUE_NAME=asbqueue
FLASK_ENV=development
FLASK_DEBUG=True

# 🔑 Authentication Methods:
# Local Development: Uses Azure CLI credentials (run 'az login' first)
# Azure App Service: Uses Managed Identity automatically
# 
# No connection strings or secrets needed! 🎉
# Note: Never commit the actual .env file to version control